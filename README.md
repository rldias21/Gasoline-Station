# ⛽ Junior Technical Challenge #1 — Gas Station

Simple application in **Java + Spring Boot**, for registering and consulting fuel prices.

## 🚀 Technologies
- Java 17
- Spring Boot
- Maven

## 📦 Project Structure
- `controller/` → controls HTTP routes
- `service/` → business logic
- `repository/` → persistence simulation (in memory)
- `dto/` → data transport
- `model/` → main entities

## 🧠 Execution
To run:
```bash
mvn spring-boot:run
